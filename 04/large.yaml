name: large
description: "Heavy load - stress testing"

tempo:
  variant: stack

storage:
  minioSize: "50Gi"

k6:
  vus:
    min: 20
    max: 100
  ingestion:
    mbPerSecond: 5
    traceProfile: large
  query:
    queriesPerSecond: 50
