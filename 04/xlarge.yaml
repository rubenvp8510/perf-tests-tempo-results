name: xlarge
description: "Extreme load - capacity testing"

tempo:
  variant: stack

storage:
  minioSize: "200Gi"

k6:
  vus:
    min: 50
    max: 200
  ingestion:
    mbPerSecond: 20
    traceProfile: xlarge
  query:
    queriesPerSecond: 100
